<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SpicaWeather" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/textColorPrimaryHint</item>
        <item name="colorPrimaryVariant">@color/textColorPrimaryLight</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Status bar color. -->
<!--        <item name="android:windowTranslucentStatus">true</item>-->
        <item name="android:statusBarColor" tools:targetApi="l">@color/window_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>
<!---->
    <style name="Theme.SpicaWeather.TranslateStatusBar" parent="Theme.SpicaWeather">
      <item name="android:windowTranslucentStatus">true</item>
    </style>


    <style name="SplashTheme" parent="Theme.SpicaWeather">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="spica_card">
        <item name="android:clickable">true</item>
        <item name="android:stateListAnimator" tools:ignore="NewApi">@animator/touch_raise</item>
    </style>

    <style name="BaseDialogStyle" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowTranslucentStatus">true</item><!-- 解决全屏时状态栏变黑 -->
        <item name="android:backgroundDimEnabled">true</item>   <!-- 是否允许背景模糊 -->
        <item name="android:windowIsTranslucent">true</item>    <!-- 是否半透明 -->
        <item name="android:windowIsFloating">true</item>       <!-- 是否浮现在activity之上 -->
        <item name="android:windowContentOverlay">@null</item>  <!-- 是否有遮盖 -->
        <item name="android:backgroundDimAmount">0.8</item>     <!-- 设置背景模糊的透明度-->
    </style>
</resources>